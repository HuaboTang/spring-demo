spring:
  cloud:
    config:
      server:
        git:
          # clone地址：https://gitee.com/bobot/spring-demo.git
          uri: https://gitee.com/bobot/spring-demo
          search-paths: spring-cloud-config/spring-cloud-config-configs/src/main/resources
          timeout: 10

      allow-override: true
